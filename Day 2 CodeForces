A. Way Too Long Words

Solution : 


import java.util.Scanner;
 
public class string {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
 
        int n = sc.nextInt();
        sc.nextLine();
 
        for(int i = 0; i < n;i++) {
            String s = sc.nextLine();
            int length = s.length();
 
            if(length > 10)
            {
                String a = s.charAt(0) + String.valueOf(length - 2) + s.charAt(length-1);
                System.out.println(a);
            } else{
                System.out.println(s);
            }
        }
        sc.close();
    }
}
